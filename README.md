# Ночь перед КР

Кооперативная 3D-игра адаптации первокурсников Института математики и информатики МПГУ. Godot 4, ENet-мультиплеер, LLM-мост на Python для педагогической мини-игры.

Полное описание сюжета, ролей, актов и NPC — в [`GAME_DESIGN.md`](GAME_DESIGN.md).

## Что уже есть

- Скелет Godot 4 проекта: главное меню, лобби, одна 3D-сцена (коридор Гаврикова с двумя тайниками), FPS-контроллер с мультиплеером через ENet
- Тайники акта 2 рабочие по сети: сбор идёт через RPC на хост, все игроки видят исчезновение тайника и попадание листка в командный инвентарь (Tab), плюс тост-уведомление кто что нашёл
- Акт 3 «сеть замков»: 4 терминала в коридоре, каждый видит свою вершину только по совпадению роли — команда обязана проговорить находки вслух, чтобы собрать код в общей панели
- Акт 4 «загадка старой аудитории»: доска в 305 (открывается только после акта 3) + стенд в вестибюле (с пасхалкой про Малую Пироговскую, 1) — две половины кода снимают замок с двери КР
- Финальная КР (акт 5) и концовки: дверь ауд. 401 в конце коридора (заперта до конца акта 4) → 3 личные задачи (аналитик/алгебраист/геометр) + командный шаг, староста собирает и сдаёт → 4 мем-концовки с фактом про МПГУ
- Игра полностью проходима от лобби до концовки: Меню → Лобби → Коридор (тайники → сеть замков → загадка 305) → КР → Концовка
- Autoload-ы: `GameState` (прогресс, роли, конспекты, педагогика), `NetworkManager` (host/join по IP), `LLMBridge` (HTTP-клиент)
- Педагогическая мини-игра «объясни школьнику» — Godot UI + Python-сервер, вызывающий Anthropic API (Claude Haiku 4.5)
- Заготовки под NPC-персонажей (Артём-старшекур, тётя Люба, куратор Валерия) в LLM-мосте

## Чего пока нет

- Настоящих фото/видео корпусов — стоят простые серые боксы (см. «Куда класть медиа»)
- Акты 3-5 живут в той же одной сцене коридора (плейсхолдер-боксы вместо отдельных помещений 305/вестибюля) — реальная геометрия появится вместе с фото
- Задачи КР и коды актов 3-4 зашиты фиксированно (без генерации через LLM для реиграбельности)
- Экрана выбора конкретной темы для педагогической мини-игры
- Хостинга LLM-моста в интернете и сервера ENet — пока только локально

## Первый запуск

### 1. Godot 4 клиент

1. Скачай [Godot 4.3+](https://godotengine.org/download) (Standard, не .NET). Проверено на 4.3 и 4.7.
2. Открой этот репозиторий как проект (`Import` → выбрать `project.godot`).
3. Запусти сцену `res://scenes/MainMenu.tscn` (F5 после установки её как главной, либо через кнопку play).

### 2. LLM-мост (Python)

Нужен, только если хочешь тестировать педагогическую мини-игру.

```bash
cd llm_bridge
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
# в .env впиши свой ANTHROPIC_API_KEY
uvicorn main:app --reload --port 8000
```

Проверка: `curl http://localhost:8000/health` → `{"status":"ok","model":"claude-haiku-4-5-20251001"}`.

### 3. Двое в кооп локально

- Запусти игру на одном компьютере → «Создать игру» → в лобби смотри «клиенты подключаются по: ...»
- На втором компьютере (в той же сети) → «Присоединиться», вводишь IP хоста
- Хост в лобби жмёт «Начать игру» — оба попадают в коридор Гаврикова, ходят WASD + мышью
- E — взаимодействие с тайниками, терминалами сети замков, досками и панелями кода; Tab — инвентарь конспектов
- Полный путь: собери тайники → каждая роль читает свой терминал сети замков и называет цифру вслух → кто-то вводит код в центральной панели (акт 3) → читай доску в 305 и стенд в вестибюле → сложи половины во второй панели (акт 4) → дверь ауд. 401 открыта → КР (акт 5)

Через интернет: нужен либо VPN (Tailscale, Hamachi), либо прокинуть порт `4242/udp` на роутере хоста, либо поднять сервер на VPS.

## Куда класть медиа

- `assets/photos/gavrikova/` — фото корпуса Гаврикова 7-9 (вход, аудитории 204, 207, 301, 302, 303, 304, 306, 307, 401, 402, 404, 405, 410, коридор)
- `assets/photos/krasnoprudnaya/` — фото корпуса Краснопрудная 14 (для пост-MVP)
- `assets/videos/` — видеообзоры для photogrammetry (тяжёлое, в .gitignore, не пушится)
- `docs/schedule/` — учебные планы и расписания в PDF

Форматы: `.jpg` / `.png` для фото, `.mp4` для видео. 360°-панорамы — `.jpg` equirectangular.

Импорт в Godot: положи файл в `assets/photos/gavrikova/aud405.jpg`, в редакторе увидишь его в FileSystem. Дальше используем как текстуру для меша стены/панорамы (это следующая задача).

## Структура репо

```
.
├── GAME_DESIGN.md              главный дизайн-документ, правки сюда
├── project.godot               конфиг Godot 4
├── autoload/
│   ├── GameState.gd            прогресс игры, роли, инвентарь
│   ├── NetworkManager.gd       ENet host/join
│   └── LLMBridge.gd            HTTP-клиент к llm_bridge
├── scripts/
│   ├── MainMenu.gd
│   ├── Lobby.gd
│   ├── Player.gd               FPS-контроллер
│   ├── Corridor.gd             спавн игроков в сцене
│   ├── Interactable.gd         базовый класс интерактивных объектов
│   ├── NoteStash.gd            тайник с листком конспекта (акт 2)
│   ├── GraphNode.gd            приватная вершина сети замков (акт 3)
│   ├── LockPanel.gd            общая панель ввода кода (акты 3-4)
│   ├── ClueSign.gd             половина подсказки, опц. за флагом (акт 4)
│   ├── ExamDoor.gd             дверь КР, заперта до act4_unlocked
│   ├── CodeEntryDialog.gd      переиспользуемое окно ввода кода
│   ├── ExamTasks.gd            условия задач финальной КР
│   ├── FinalExam.gd            экран КР (акт 5)
│   ├── Ending.gd                концовки по оценке
│   ├── Hud.gd                  тосты + инвентарь (автозагрузка)
│   └── ExplainDialog.gd        педагогическая мини-игра
├── scenes/
│   ├── MainMenu.tscn
│   ├── Lobby.tscn
│   ├── Player.tscn
│   ├── CorridorGavrikova.tscn  стартовая 3D-сцена (акты 1-4)
│   ├── CodeEntryDialog.tscn
│   ├── FinalExam.tscn
│   ├── Ending.tscn
│   ├── Hud.tscn
│   └── ExplainDialog.tscn
├── llm_bridge/                 Python FastAPI мост к Anthropic
│   ├── main.py
│   ├── requirements.txt
│   ├── Dockerfile
│   └── .env.example
├── assets/                     фото, видео, звуки (в .gitignore крупняк)
└── docs/
    └── schedule/               PDF учебных планов
```

## Кто что делает

- **Ты (человек):** открываешь Godot Editor, тестируешь визуально, снимаешь фото/видео корпусов, кладёшь их в `assets/`, согласуешь с деканатом использование имён/фактов.
- **Claude (в этой сессии):** пишет и правит GDScript/`.tscn`/Python, генерирует задачи для КР, обновляет `GAME_DESIGN.md`. Открыть редактор Godot из этой сессии не могу.

Если хочешь, чтобы Claude работал прямо на твоей машине (быстрее и не надо гонять через git) — поставь себе [Claude Code CLI](https://claude.com/claude-code) локально: `npm install -g @anthropic-ai/claude-code`, запусти `claude` в корне репо. Все возможности этой сессии + твоя файловая система под рукой.

## Дорожная карта

Задачи ведутся в этой сессии (`TaskList`). Крупными штрихами:

1. ✅ Дизайн-док
2. ✅ Скелет Godot 4 и мультиплеер
3. ✅ LLM-мост
4. ⏳ Актуализировать сцену коридора реальными фото Гаврикова
5. ✅ Реализовать акты 3-4 — сеть замков, загадка старой аудитории (первая версия, фикс. коды, всё в одной сцене-коридоре)
6. ✅ Финал: КР с задачами по специализациям + мем-эндинги (первая версия, фикс. задачи)
7. ⏳ Полный NPC-разговор (Артём) с UI-диалогом в игре
8. ⏳ Оффлайн-версия «карточки для команды в столовой» — PDF-квесты
9. ⏳ Деплой LLM-моста + ENet-сервера на VPS

Обновляется по мере хода работы.
